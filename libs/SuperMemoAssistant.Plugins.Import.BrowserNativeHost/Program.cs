using SuperMemoAssistant.Extensions;

namespace SuperMemoAssistant.Plugins.Import
{
  class Program
  {
    /// <summary>
    /// This process will be instantiated by the browser.
    /// First argument should be the plugin id (e.g. chrome-extension://)
    /// Second argument should be the handle of the calling browser (e.g. --parent-window=[decimal handle value])
    /// </summary>
    /// <param name="args"></param>
    static async void Main(string[] args)
    {
      
    }


  }
}
